Integration scripts for manual integration with OSPd deployed Overcloud.

Run as root.

Make sure to edit the variables in the scripts.

Make the packages available in /opt/nuage

Edit the vlans when needed.
